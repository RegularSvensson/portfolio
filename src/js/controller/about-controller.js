define(
  [
    'angular',
  ],
  function(angular) {
    'use strict';

    angular
      .module('ng-boilerplate.about-controller', [
      ])
      .controller('AboutController', [
        '$scope',
        function($scope) {
          
        }
      ]);
  }
);